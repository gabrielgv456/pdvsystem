import styled from "styled-components";

export const ButtonPeriod = styled.button `
    color: var(--AppBar);
    font-size: 0.65rem;
    border: none;
    background: none;
    align-items: center;
`